#include "tokenizer.hpp"

namespace stina
{
    std::vector<token::Token> tokenize(std::string data)
    {
        std::vector<token::Token> tokens;
        for(char c : data)
        {
            
        }
    }
}
